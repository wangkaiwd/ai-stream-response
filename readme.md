## AI 流失响应示例

* 基础知识在 `examples`
* 完整的示例在 `apps/express`


### 快速开始

* `pnpm install` 安装依赖
* `cd apps/express` 进入 express 目录
* 重命名 `.env.example` 为 `.env`，并配置`OPENAI_API_KEY` 和 `OPENAI_BASEURL`
* 启动服务：`pnpm run sse:ai`
